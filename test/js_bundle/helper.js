// Helper module for testing JavaScript bundling
export const HELPER_VERSION = "1.0.0";

export function formatMessage(prefix, message) {
    return `[${prefix}] ${message}`;
}

export function uppercase(str) {
    return str.toUpperCase();
}
